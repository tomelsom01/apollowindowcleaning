<div class="container mt-2 pb-4">
  <div class="row">
    <div class="col-sm-6 card">
      <p>Please complete the form and we’ll give you a call so you can ask us any questions you might have.</p>
      <p>We look forward to hearing from you and about your business.</p>
      <p>T: 07832-355-491</p>
      <p>E: apollowindowcleaning124@gmail.com</p>
    </div>
    <div class="contactform col-sm-6">
      <%= form_with model: @contact, url: create_contact_path, local: true do |f| %>
        <div class="form-group text-white">
          <%= f.label :name %>
          <%= f.text_field :name, class: 'form-control', placeholder: 'Name' %>
        </div>
        <div class="form-group text-white">
          <%= f.label :email %>
          <%= f.email_field :email, class: 'form-control', placeholder: 'johndoe@email.com' %>
        </div>
        <div class="form-group text-white">
          <%= f.label :phone %>
          <%= f.text_field :phone, class: 'form-control', placeholder: 'Enter your phone number' %>
        </div>
        <div class="form-group text-white pb-2">
          <%= f.label :message %>
          <%= f.text_area :message, class: 'form-control', placeholder: 'Your message' %>
        </div>
        <%= f.submit 'Send', class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
</div>
